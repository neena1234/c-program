#include<stdio.h>
main()
{
int b,h,a;
printf("enter the length of tiangle");
scanf("%d",&b);
printf("enter the length of hight triangle");
scanf("%d",&h);
a=0.5*b*h;
printf("area of triangle is %d",&a);
}

